---
# defaults file for roles/users

# Users that should be created. Override in group_vars/host_vars per environment.
users:
  - name: deployer
    groups: []
    sudo: true

# SSH public keys, keyed by username.
user_ssh_keys:
  deployer: ""
